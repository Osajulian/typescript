let a: number;
let b: string;
let c: boolean;
let d: any;
let e: number[] = [1, 2, 4];
let f: string[] = ['one', 'two', 'three'];
let g: any[] = [1, 'two'];

// javascript

const colorRed = 0;
const colorWhite = 1;
const colorBlue = 2;

//using Typescript enums

enum color {Red = 0, White = 1, Blue = 2};

// use
let backgroundColor = color.Blue;